{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main, About, and Update windows",
    "windows": ["main", "about", "update"],
    "permissions": [
      "core:default",
      "core:window:allow-start-dragging",
      "core:window:allow-close",
      "core:app:allow-version",
      "dialog:default",
      "opener:default"
    ]
}
